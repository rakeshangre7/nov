{
  "name": "nov",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "engines": {
    "node": ">=18",
    "npm": ">=7",
    "yarn": "yarn is not supported, please use npm"
  },
  "scripts": {
    "install-all": "npm install && cd src && cd nov && npm install",
    "precommit:app": "cd src && cd nov && npm run lint:staged",
    "precommit": "npm-run-all precommit:*",
    "prepush:app": "cd src && cd nov && jss bootstrap && npm run test:silent && echo '' && echo '> running typescript compilation' && npx tsc --project tsconfig.json",
    "prepush": "npm-run-all prepush:*",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NationalOilwellVarco/nov-com-xm-cloud.git"
  },
  "author": "Horizontal Digital",
  "devDependencies": {
    "@commitlint/cli": "^13.1.0",
    "commitlint-config-jira": "^1.5.1",
    "commitlint-plugin-jira-rules": "^1.5.1",
    "husky": "^7.0.1",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "all": "^0.0.0",
    "axios": "^0.27.2"
  }
}
